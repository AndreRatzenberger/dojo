# H18

## TASK

Translate the partial evidence below from a short video into an original static public-information poster system for municipal cooling centers.

Available visual samples:

- `FRAME-00:03`: Wide shot. A sunlit bus shelter fills the frame. A white notice occupies the upper-left pane; three orange rectangles form a vertical stack. No notice text is legible.
- `FRAME-00:11`: Close shot. A hand points to a street map. A thick blue route bends toward a solid orange circle in the lower-right quadrant.
- `FRAME-00:19`: Medium shot. Four people sit beneath a narrow band of shade. Water bottles align along the bottom edge. The upper half is mostly empty pale sky.

Available transcript excerpt:

- `LINE-01 [00:07–00:10]`: “Find the nearest open cooling center before the afternoon peak.”
- `LINE-02 [00:12–00:16]`: “Bring identification if you have it, but entry is available without it.”
- `LINE-03 [00:17–00:20]`: “Check today’s hours and accessible-route information.”

Audio qualities, music, ambient sound, vocal delivery, motion between the sampled frames, transitions, and the rest of the video are unavailable. Make no claims about them.

Produce:

1. A clearly labeled `provisional` taste profile.
2. An axis-accounting table covering `composition`, `color`, `information hierarchy`, `imagery`, `typography`, `motion`, and `sound`. Every row must be labeled `supported`, `partial`, or `unknown` and must include either a precise evidence coordinate—frame ID plus spatial region or transcript line plus timestamp—or an explicit inspected boundary explaining where the unknown was checked.
3. A limitation statement that forbids claims about unavailable audio, motion between frames, transitions, off-sample visuals, or the remainder of the video.
4. A function-based cross-medium translation into a static poster system. Translate evidence by communicative function—such as directing attention, establishing destination, preserving breathing room, or clarifying access—not by imitating shots or reconstructing a storyboard.
5. An implementation handoff covering poster grid, hierarchy, type roles, color roles, map/destination treatment, access statement, hours and accessibility modules, placement variants, and traceability to evidence coordinates.
6. The smallest additional-evidence request set needed to resolve the most consequential `partial` or `unknown` axes. For each request, name the uncertainty it resolves and why that uncertainty affects the poster; do not request evidence that would not change a design decision.
