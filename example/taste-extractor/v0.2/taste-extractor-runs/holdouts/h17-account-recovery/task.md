# H17

## TASK

Use these three supplied passages:

- `PASSAGE-A`: “Mara checked the latch twice, then placed the spare key beneath the blue cup. By dusk, the cup had moved.”
- `PASSAGE-B`: “The clerk read the name aloud. I corrected one letter. He closed the ledger and asked for the seal.”
- `PASSAGE-C`: “Only one ferry remained. Tomas unfolded the rain-soft ticket and saw yesterday’s date bleeding through the paper.”

Create consequential account-recovery interface copy for a regional credit-union app.

First, build a complete passage-by-axis matrix: three passage rows by exactly these six axes—`voice`, `rhythm`, `specificity`, `tension`, `agency`, and `consequence`. Every one of the 18 cells must contain an interpretation and a supporting verbatim quote from its passage.

Then write copy for exactly three recovery moments:

1. `IDENTITY_CHECK` — the user must prove account ownership.
2. `SECURITY_HOLD` — suspicious recovery activity has triggered a protective delay.
3. `FINAL_WARNING` — one remaining attempt precedes a temporary lock.

Use literal identity, security, and consequence language. Do not rely on metaphor to communicate what is being verified, why access is delayed, or what happens after failure.

Apply this exact precedence statement and reproduce it verbatim in the response:

> Security and comprehension override literary resemblance; when they conflict, preserve the literal instruction and remove the stylistic echo.

For each moment, use the same exact twelve-field contract, in this order:

1. `moment_id`
2. `source_trace_ids`
3. `user_state`
4. `eyebrow`
5. `headline`
6. `body`
7. `primary_action`
8. `secondary_action`
9. `consequence`
10. `support_path`
11. `accessibility_note`
12. `rationale`

Populate every field for every moment. In each `rationale`, connect copy choices to matrix evidence without borrowing plot, names, objects, or distinctive phrasing from the passages.
