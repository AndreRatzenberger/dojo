# T1 baseline score

Result: 5/5 observable checks passed.

| # | Pass | Evidence |
|---|---|---|
| 1 | yes | `Direct source evidence` is separated from `Derivation trace` and `Taste abstractions`. |
| 2 | yes | The output covers grid/composition, typography, color, density, motion, and state interaction. |
| 3 | yes | `Must`, `Should`, `Avoid`, and `Unresolved variables` separate fixed rules from adaptable choices. |
| 4 | yes | The anti-pattern inventory and unresolved-variable rules prevent shallow or fabricated copying. |
| 5 | yes | The downstream contract ends with criterion-shaped verification checklists. |

Baseline finding: this runner already produced a rigorous interface handoff.
T1 contributes no failure for skill authoring; it remains a regression case to
ensure the candidate does not make strong default behavior worse.
