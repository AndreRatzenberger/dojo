# Original request

> With Dojo, create a skill that extracts "taste" out of any medium so other
> agents can produce entities of that medium in the same style.

The example names the resulting skill `taste-extractor` and treats it as a
technique-tier skill because it orchestrates observation, abstraction,
translation, and a downstream production handoff.
